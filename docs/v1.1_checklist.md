# MoDES v1.1 Development Checklist

All items below are partially complete (⚠️) or not started (❌) as of `518b3d9`.

---

## 1. pyproject.toml — migrate from setup.py

- [x] Add `pyproject.toml` as primary build entry point
- [ ] Test `python -m build` succeeds (CI)
- [ ] Test `pip install dist/*.whl` succeeds (CI)
- [ ] Test `modes --help` works after wheel install (CI)
- [x] Keep `setup.py` as fallback during transition

## 2. CI / Packaging

- [x] Add `build` job to `.github/workflows/tests.yml` (wheel build + twine check + fresh install test)
- [x] Add `modes --help` smoke test to CI
- [ ] Add Python 3.12 back when stable

## 3. validate-input — JSON output

- [x] `modes validate-input --out validation.json` produces machine-readable JSON
- [x] JSON schema: `{ok, errors, warnings, n_samples, n_genes, n_peaks, n_links, n_links_matched}`
- [x] CLI validate-input and Python API share same validator (`MoDEData.validate()`)
- [ ] Return `ValidationReport` dataclass instead of raw `list[str]`

## 4. Output schema freeze

- [x] Freeze `event_table.tsv` column order (25 fields)
- [x] Add `test_event_table_schema_exact` test
- [x] Verify `docs/output_schema.md` matches actual output columns exactly

## 5. Enhanced `filter()`

- [x] `genes` filter (list of gene names)
- [x] `peaks` filter (list of peak IDs)
- [x] `context` filter (string or list)
- [x] `states` filter (list of states)
- [x] Unit test for each new filter parameter

## 6. `save()` / `load()`

- [x] `result.save("output/")` — save all tables
- [x] `MoDESResult.load("output/")` — reload from directory
- [x] Round-trip test

## 7. Run params completeness

- [x] Add to `run_params.tsv`/`run_params.json`: MoDES version, Python version, numpy/pandas/statsmodels versions
- [x] Add: `n_external_links`
- [ ] Add: `runtime_seconds`

## 8. Artifact risk — enhanced reasons

- [x] `low_atac_depth`
- [x] `low_rna_depth`
- [x] `low_quality_score`
- [x] `borderline_quality`
- [x] `single_modality_low_quality`
- [ ] `library_size_outlier`
- [ ] `batch_associated`
- [ ] `weak_link_score`
- [ ] `low_group_replicates`
- [ ] Decompose `quality_score` into sub-scores

## 9. cis_ATAC_score — gene-level aggregation

- [ ] Build `cis_ATAC_score(g) = weighted sum of linked peaks for gene g`
- [ ] Weight options: distance decay, uniform, external link score
- [ ] New parameter: `conditional_mode = "single_peak" | "cis_score"`
- [ ] Output: `rna_after_cis_atac_coef`, `cis_atac_score_method`

## 10. event_id — deterministic

- [x] `event_id = sha1(peak_id|gene|source)[:12]`
- [x] Same input → same event_id every run
- [ ] Also output `event_key` for human readability

## 11. External links — schema hardening

- [x] Normalize `link_source`: promoter, distal_250kb
- [ ] Output `link_score` (0–1) and `link_distance` in event_table
- [x] Validate link_score range in `validate_external_links()`

## 12. from_pseudobulk — full metadata

- [ ] Output `obs` columns: `group_id`, `donor`, `condition`, `cell_type`, `n_cells`, `rna_total_counts`, `atac_total_counts`
- [x] Drop groups below `min_cells_per_group` and warn
- [ ] Tests for: groupby column missing, condition_col missing, donor_col missing, empty groups, ATAC layer missing, cell order mismatch

## 13. `run_by_context()` — multi-cell-type batch

- [ ] `run_by_context(data, context_col="cell_type", min_samples_per_context=4)`
- [ ] Output: `all_contexts_event_table.tsv`, `context_summary.tsv`
- [ ] No user for-loop needed

## 14. Spatial region-pseudobulk

- [ ] `MoDEData.from_spatial_pseudobulk(rna, atac, metadata, region_col, sample_col, condition_col)`
- [ ] Output `context = spatial_region`
- [ ] Document: "spatial in v1.1 = region-pseudobulk, not native graph"

## 15. Benchmarks — hardening

- [ ] Add `--quick` mode to all benchmark scripts
- [ ] CI runs quick benchmarks (not full)
- [ ] Standardize output: `truth.tsv`, `predicted.tsv`, `metrics.tsv`, `confusion_matrix.tsv`, `confusion_matrix.png`, `runtime.tsv`
- [ ] Negative control: add shuffle-links, shuffle-ATAC, random-links variants
- [ ] Baseline comparison: per-state precision/recall, artifact risk specificity

## 16. Biological demo

- [ ] Find a dataset with real biological condition (differentiation / stimulation / treatment)
- [ ] Run MoDES, verify chromatin_primed events enrich lineage TFs
- [ ] Verify concordant events enrich mature markers
- [ ] Verify rna_only events enrich stress/trans pathways
- [ ] Output: `demo_event_table.tsv`, `motif_enrichment.tsv`, `marker_enrichment.tsv`

## 17. CITATION.cff

- [ ] Update author from "MoDES contributors" to real name
- [ ] Add ORCID if available

## 18. Sparse matrix support (future)

- [ ] `from_pseudobulk()` handles `scipy.sparse.csr_matrix` input
- [ ] Internal pseudobulk aggregation on sparse matrices
- [ ] Document: v1.x GLM uses dense pseudobulk; cell-level sparse only for aggregation

## 19. MuData support (future)

- [ ] `MoDEData.from_mudata(mdata, rna_mod, atac_mod, groupby, ...)`
- [ ] Two modes: groupby=None (paired cells), groupby=[...] (pseudobulk)
- [ ] Toy MuData test

---

## Progress Summary

| Category | Items | Done |
|---|---|---|
| Packaging & CI | #1, #2 | 1.5/2 |
| Input validation | #3 | 0.75/1 |
| Output schema | #4 | 1/1 |
| API | #5, #6, #7, #10 | 3.5/4 |
| Statistics | #8, #9 | 0.5/2 |
| Event links | #11 | 0.67/1 |
| Single-cell | #12, #13 | 0.5/2 |
| Spatial | #14 | 0/1 |
| Benchmarks | #15 | 0/1 |
| Real data | #16 | 0/1 |
| Docs | #17 | 1/1 |
| Future | #18, #19 | 0/2 |
| **Total** | **19** | **~55%** |
